# Dependencies & build
node_modules
dist
dist-ssr
*.local

# Environment (never commit real keys)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea

# VS Code — keep optional shared recommendations, ignore the rest
.vscode/*
!.vscode/extensions.json

# Cursor IDE — local rules / agent state (canonical docs: PROJECT_CONTEXT.md, docs/)
.cursor

# Supabase CLI (local)
supabase/.temp
.branches

# Tooling / caches
*.tsbuildinfo
.cache
.turbo
coverage
*.pem

# Optional local notes (uncomment if you use them)
# .env.example is usually committed; real secrets stay in .gitignore above
